<Window x:Class="GazoView.Lib.Panel.DeleteMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GazoView.Lib.Panel"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner"
        PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown"
        PreviewKeyDown="Window_PreviewKeyDown"
        Height="240" Width="580">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Noto Sans JP" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="LightGray" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ButtonContent" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="AliceBlue" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="DimGray" />
                                <Setter TargetName="ButtonContent" Property="TextElement.Foreground" Value="White" />
                                <Setter TargetName="ButtonContent" Property="TextElement.FontWeight" Value="Bold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="FontFamily" Value="Noto Sans JP" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>

    <Grid Margin="5">
        <Border CornerRadius="15" Background="LightGray">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.2" />
            </Border.Effect>
        </Border>
        <Border CornerRadius="0,0,15,15" Background="Gray" Height="48" VerticalAlignment="Bottom" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" HorizontalAlignment="Left"
                    CornerRadius="15" Background="#F3F3F3" Width="180" Height="180" Margin="25,20">
                <Image Name="TargetImage" Margin="10" >
                    <Image.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.2" />
                    </Image.Effect>
                </Image>
            </Border>
            <StackPanel Grid.Column="1" 
                        Orientation="Vertical">
                <Grid Margin="0,25">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="23"/>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Name="TextBlockAction" Text="Delete?" Grid.Row="0" Grid.ColumnSpan="4" FontWeight="Bold" />
                    <TextBlock Name="TextBlockFilePath" Text="C:\test\Sample\example.jpg" Grid.Row="1" Grid.ColumnSpan="4" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Name" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Extension" />
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="Image Size" />
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="File Size" />
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="TimeStamp" />
                    <TextBlock Grid.Row="2" Grid.Column="2" Text=":" />
                    <TextBlock Grid.Row="3" Grid.Column="2" Text=":" />
                    <TextBlock Grid.Row="4" Grid.Column="2" Text=":" />
                    <TextBlock Grid.Row="5" Grid.Column="2" Text=":" />
                    <TextBlock Grid.Row="6" Grid.Column="2" Text=":" />
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="Example.jpg" Name="TextBlockFileName"/>
                    <TextBlock Grid.Row="3" Grid.Column="3" Text=".jpg" Name="TextBlockFileExtension"/>
                    <TextBlock Grid.Row="4" Grid.Column="3" Text="1920x1080" Name="TextBlockImageSize"/>
                    <TextBlock Grid.Row="5" Grid.Column="3" Text="1.2 MB" Name="TextBlockFileSize"/>
                    <TextBlock Grid.Row="6" Grid.Column="3" Text="2024-06-01 12:34:56" Name="TextBlockTimeStamp"/>
                </Grid>
            </StackPanel>
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal"
                        VerticalAlignment="Bottom"
                        Height="48"
                        Margin="30,0">
                <Button Name="ButtonOK" Content="OK" />
                <Button Name="ButtonCancel" Content="Cancel" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
